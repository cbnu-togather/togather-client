<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Button background -->
    <item android:id="@android:id/background">
        <shape android:shape="rectangle">
            <solid android:color="@color/white" /> <!-- 변경된 부분 -->
        </shape>
    </item>
    <!-- Bottom border -->
    <item android:bottom="-10dp" android:right="-10dp" android:left="-10dp">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
            <stroke
                android:width="0.5dp"
                android:color="@color/tab_top_horizontal_line_gray_color" />
        </shape>
    </item>
</layer-list>
